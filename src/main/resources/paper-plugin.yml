name: 'TeleportAssist'
version: '0.3.0'
main: 'org.winlogon.teleportassist.TeleportAssist'
api-version: 1.21
description: 'Paper plugin providing async teleport requests with Folia compatibility'
folia-supported: true
dependencies:
  server:
    ScalaLoader:
      load: BEFORE
      required: true
      join-classpath: true
